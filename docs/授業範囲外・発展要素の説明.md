# Gitぱっち 発展要素 学習シート

## このシートの目的

Gitぱっちの現在のコードには、授業で学んだ基本文法に加えて、少し発展したJavaの記法やAPIが使われている。このシートでは、それぞれの役割と基本文法とのつながりを確認する。

> このファイルは `docs/` にあるため、Gitで管理される。授業参考資料は `docs/ex/Java授業参考資料/` を参照する。

---

> ## 学習カード 1：授業で学んだ土台
>
> **学ぶこと：クラスの役割分担、カプセル化、制御文、例外処理、コレクション**

Gitぱっちは、授業で学んだJavaの基本を組み合わせて作っている。

`Pet` はペットの状態を持つクラス、`PetService` は状態を変更するルールを持つクラス、`Menu` は画面表示と入力を担当するクラスである。この分け方は、`AI展開資料/08-oop-classes.md` の「クラスごとに役割を分ける」考え方に対応している。

`Pet()` や `RepositoryProfile(String, String)` はコンストラクタ、`private` フィールドとgetterはカプセル化、`if`・`while`・`switch` は制御文、`try-catch` は例外処理、`List<RepositoryProfile>` と `ArrayList` はコレクションの学習内容である。

**確認ポイント：** 発展した書き方を使っていても、アプリの基本は授業で学んだJavaでできている。

---

> ## 学習カード 2：`enum` で表情を管理する
>
> **学ぶこと：決まった候補を型として扱う**

**使っているコード：** `src/Pet.java`

```java
public enum Expression {
    HUNGRY, SAD, HAPPY, NORMAL
}
```

`enum` は、決まった候補だけを扱うための型である。Gitぱっちでは、ペットの表情を `HUNGRY`、`SAD`、`HAPPY`、`NORMAL` の4種類に限定している。

文字列で表情を管理すると、`HAPPY` のつもりで `HAPYY` と書いても、Javaはすぐに間違いだと判断できない。`enum` を使うと、存在しない表情名はコンパイルエラーになる。

```java
Pet.Expression expression = Pet.Expression.HAPPY;
```

**授業とのつながり：** クラス・型・カプセル化の考え方を発展させた書き方。

**確認ポイント：** 表情の追加やGUI化をするときも、表情の種類を1か所で管理できる。

---

> ## 学習カード 3：矢印形式の `switch`
>
> **学ぶこと：分岐の書き忘れを減らす**

**使っているコード：** `src/Menu.java`

```java
switch (input) {
case "1" -> showPetStatus();
case "2" -> feedPet();
case "3" -> strokePet();
default -> System.out.println("0〜3の番号を入力してください。");
}
```

授業でよく使う書き方では、`case:` と `break` を使う。

```java
switch (input) {
case "1":
    showPetStatus();
    break;
default:
    System.out.println("0〜3の番号を入力してください。");
    break;
}
```

矢印形式では、`break` を書き忘れて次のcaseまで実行してしまう失敗を防ぎやすい。Java 21で使える書き方である。

また、`faceOf` と `statusMessageOf` では、`switch` の結果を直接返している。

```java
return switch (expression) {
case HUNGRY -> "(´；ω；`)";
case SAD -> "(｡•́︿•̀｡)";
case HAPPY -> "(＾▽＾)";
case NORMAL -> "(・ω・)";
};
```

**授業とのつながり：** `switch` は `AI展開資料/05-control-flow.md` の内容。矢印形式はJava 21の発展的な書き方。

---

> ## 学習カード 4：`Math.clamp` で状態を0〜100に保つ
>
> **学ぶこと：値の範囲を守る**

**使っているコード：** `src/Pet.java`

```java
this.hunger = Math.clamp(hunger, 0, 100);
```

`Math.clamp(値, 最小値, 最大値)` は、値を指定範囲に収めるJava 21のAPIである。満腹度が `-10` なら `0`、`120` なら `100` になる。

同じ処理は、授業で学ぶ `if` と比較演算子だけでも書ける。

```java
if (hunger < 0) {
    this.hunger = 0;
} else if (hunger > 100) {
    this.hunger = 100;
} else {
    this.hunger = hunger;
}
```

**授業とのつながり：** 比較演算子と `if` は `AI展開資料/04-operators-input.md`、`05-control-flow.md` の内容。

**確認ポイント：** `Math.clamp` は短く書くための便利APIであり、考え方の中心は条件分岐である。

---

> ## 学習カード 5：`final` で参照の入れ替えを防ぐ
>
> **学ぶこと：途中で変えてはいけないものを表す**

**使っているコード：** `src/Menu.java`

```java
private final Scanner scanner;
private final Pet pet;
private final PetService petService;
```

`final` を付けると、コンストラクタで受け取った `Pet` や `PetService` を、あとから別のインスタンスに入れ替えられない。

これは「このMenuは、作られたときに渡されたペットとサービスを使い続ける」という約束をコードで表している。

ただし、`final Pet pet` はペットの満腹度や機嫌まで変わらないという意味ではない。変更できないのは、`pet` が指すペットを別のペットへ入れ替えることである。

**確認ポイント：** `final` は「オブジェクトの中身を変更不可にする」ものではなく、「参照の再代入を禁止する」もの。

---

> ## 学習カード 6：`List.copyOf` で一覧を守る
>
> **学ぶこと：Serviceの内部データを外部から守る**

**使っているコード：** `src/RepositoryService.java`

```java
public List<RepositoryProfile> findAll() {
    return List.copyOf(repositories);
}
```

`List` と `ArrayList` はコレクションの授業内容である。`List.copyOf` は、Serviceが持つ本物の一覧を直接渡さず、コピーを返すための書き方である。`RepositoryService` は、登録・更新・削除の結果を `RepositoryStatus` enumで返し、`false` だけでは分からない失敗理由を呼び出し側へ渡せるようにしている。

こうしておくと、画面側のコードが一覧を直接追加・削除しても、`RepositoryService` の内部一覧は変わらない。登録・編集・削除は、`RepositoryService` のメソッドに集められる。現在の `Menu` からはリポジトリ管理サブメニューを通じて登録・一覧表示・編集・削除を操作できる。削除時は確認入力を受け取り、登録情報だけを削除する。

**授業とのつながり：** `List`、`ArrayList`、ジェネリクスは `AI展開資料/11-collections.md` の内容。`List.copyOf` はカプセル化をさらに意識した発展的な使い方。

---

> ## 学習カード 7：`MainTest` でテスト起動する
>
> **学ぶこと：通常起動とテスト起動を分ける**

**使っているコード：** `src/MainTest.java`

通常起動は `Main`、テスト起動は `MainTest` とクラスを分けている。`MainTest` では `PetService`、`RepositoryService`、`GitActivityService`、`ActivityStreakService` の簡易テストを実行する。

```sh
# コンパイル
javac --release 21 -d /tmp/gitpatch-java21 src/*.java

# テスト起動
java -cp /tmp/gitpatch-java21 MainTest

# 通常起動（上のコンパイル結果を使う）
java -cp /tmp/gitpatch-java21 Main
```

テストでは、期待値と実際の値が違うときに `AssertionError` を出して止める。

```java
throw new AssertionError(testName + "：期待値=" + expected + "、実際=" + actual);
```

**確認ポイント：** これはJUnitを使わずに、今の段階で動作確認をしやすくするための簡易テストである。

---

> ## 学習カード 8：外部コマンドを実行する `ProcessBuilder`
>
> **学ぶこと：JavaからGitコマンドを実行する**

`GitActivityService` では、JavaからGitコマンドを実行するために `ProcessBuilder` を使う。

```text
Java
  ↓
git -C <path> log -1 --format=%H
  ↓
最新コミットIDを取得する
```

Gitコマンドは1つの長い文字列ではなく、要素ごとに渡す。

```java
new ProcessBuilder(
        "git",
        "-C",
        path,
        "log",
        "-1",
        "--format=%H"
);
```

`start()` でコマンドを実行し、`waitFor()` で終了を待つ。終了コードが `0` なら成功、`0` 以外なら失敗として扱う。

現在の `GitActivityService` は、`GitStatus` enumで結果を表す。`SUCCESS`、`EMPTY_PATH`、`PATH_NOT_FOUND`、`NOT_GIT_REPOSITORY`、`NO_COMMIT`、`GIT_COMMAND_FAILED`、`INTERRUPTED` を使い、booleanやnullだけでは分からない失敗理由をMenuへ渡せるようにしている。

> ## 学習カード 9：Gitの出力を `BufferedReader` で読む
>
> **学ぶこと：外部コマンドが出力した文字をJavaで受け取る**

Gitの最新コミットIDは、Gitコマンドの標準出力に表示される。

```text
Gitコマンド
  ↓ 標準出力
76a2ebf123456789abcdef...
```

Javaでは、次のコードでその出力を1行ずつ読めるようにする。

```java
BufferedReader reader = new BufferedReader(
        new InputStreamReader(process.getInputStream())
);
```

役割は次のとおり。

```text
process.getInputStream()
  → Gitが出力したバイト列を受け取る出口

InputStreamReader
  → バイト列を文字として読める形へ変換する

BufferedReader
  → readLine()で1行ずつ取り出せるようにする
```

実際にコミットIDを受け取る処理はこれである。

```java
String output = reader.readLine();
```

`trim()` を使うと、出力の前後にある空白や改行を取り除ける。

```java
output = output.trim();
```

たとえると、Gitコマンドが自動販売機で、`process.getInputStream()` が商品の出口、`InputStreamReader` と `BufferedReader` が商品を文字として受け取りやすくする仕組みである。

Gitの概念は `AI展開資料/01-git-github.md` を参照する。Javaから外部コマンドを実行する部分は、授業の基礎から発展した内容である。

### 発展カード：検索フォルダからGitリポジトリを探す

GitHub URLに対応するcloneを自動で見つける場合でも、PC全体を無制限に検索するのではなく、利用者が登録した検索フォルダの配下だけを対象にする。`Files.walkFileTree`でディレクトリをたどり、`.git`ディレクトリまたは`.git`ファイルを持つフォルダを候補にする。

候補を見つけた後にだけ、`git -C <候補>`で`remote.origin.url`を取得する。フォルダ名ではなくremote URLを比較することで、同名リポジトリや複数cloneを区別できる。権限のないフォルダは検索全体を止めずにスキップし、候補が複数ある場合は利用者に選択してもらう。

この機能はまだ未実装である。検索フォルダの保存、シンボリックリンク、worktree、除外フォルダ、検索深度、macOSのアクセス権を決めてから実装する。GitHub APIによる直接確認や自動cloneは、この検索機能とは別の発展案として扱う。

`CsvStorage` は現在空のクラスで、まだ未実装である。起動時の復元・終了時の保存も `Main` にはない。実装時には、ファイルの読み書き、文字列分割、数値変換、`IOException`、壊れたデータへの対応が必要になる。例外処理の基礎は `AI展開資料/10-exceptions.md` を参照する。

---

> ## 発展企画：次回の活動同期で報酬を確定する
>
> **活動同期への接続、連続記録のCLI表示、称号・節目メッセージは実装済みである。**
>
> 現在の `Menu` は、Git活動確認で前回と異なるコミットIDを見つけると、その場で30XPを付与して `lastCheckedCommitId` を更新する。CLIではコミットが作られた瞬間を監視できないため、将来は「コミットの瞬間」ではなく「次回の活動同期」で報酬を確定する方式にする。
>
> 日付単位の連続記録も、活動同期で確定した活動日を使って管理する。
>
> - 同じコミットは **0XP** とする。
> - 同じ日に別のコミットがあれば、通常XPとして **30XP** を付与する。
> - 前回の活動日が前日なら `currentStreak` を1増やす。
> - 活動日の間に空白があれば `currentStreak` をリセットする。
> - `lastActivityDate` に最後の活動日を、`longestStreak` に最長記録を保存する。
> - 連続3日で「Git見習い」、7日で「Gitマン」、14日で「Gitスター」を表示する。
> - 連続が切れたときは「今日はGitさわれなかったのかな? また1から頑張ろう！」と表示する。
>
> 実装を複雑にしすぎないため、最初はXP30固定とする。現在は `ActivityStreak` と `ActivityStreakService` が初回・同日・翌日・空白日・過去日の判定と最長記録更新を担当し、`Menu` が連続活動、称号、節目・記録切れメッセージを表示する。`MainTest` で基礎ケース、称号境界、Git活動確認の主要経路を確認済みで、CSV保存は未完了である。

---

> ## 次に進む作業
>
> **現在の実装済み・仕様案・未テスト・未実装を分けて確認する。**

**実装済み**

- `Main` は `Pet`、`PetService`、`GitActivityService`、`RepositoryService` を生成して `Menu` へ渡す。
- `Menu` はペット表示、お世話、リポジトリ管理サブメニュー（登録・一覧・編集・削除）、確認対象リポジトリの選択、Git活動確認を提供する。
- 新しいコミットと判定した場合、`Menu` は `PetService.gainExperience(pet, 30)` を呼び、その後に `lastCheckedCommitId` を更新する（現在は確認時に即時確定）。
- `RepositoryService` の登録・一覧・編集・削除と、`GitActivityService` のGit判定・最新コミットID取得・比較は実装されている。
- `ActivityStreak` と `ActivityStreakService` の基本判定、Git活動確認との接続、および `MainTest` の初回・同日・翌日・空白日・最長記録・過去日のstreakテストは実装されている。

**これから実装するもの**

- CSV保存・復元と、再起動後のstreak復元を追加する。
- `Menu.start` のGit活動以外も含む全体統合テストを追加する。
- 検索フォルダの登録、`.git`を持つclone候補の検索、remote URLによる一致判定を追加する。

連続記録のCSV永続化は、CSV保存・復元を実装した後に行う。

**`MainTest` で確認済み**

- `PetService` の経験値、レベルアップ、満腹度上限、負の経験値の無視。
- `RepositoryService` の登録、重複・空入力の検証、更新、削除。
- `GitActivityService` の `SUCCESS`、`EMPTY_PATH`、`PATH_NOT_FOUND`、`NOT_GIT_REPOSITORY`、コミットID比較、GitPatch自身のコミットID取得。
- `ActivityStreakService` の初回・同日・翌日・空白日・最長記録・過去日のstreak更新。
- `ActivityStreakService` の称号境界（0、2、3、6、7、13、14、15日）。
- `Menu.start` の新規コミット、同じコミット、Gitエラー、取消、同日別コミット、3日目、空白日、節目後の表示。

**未テストまたは未実装**

- `Menu.start` の登録・お世話・編集・削除までを含む対話操作全体は自動テストしていない。
- `NO_COMMIT`、`GIT_COMMAND_FAILED`、`INTERRUPTED` の各状態は `MainTest` で確認していない。さらに現在の `runGitCommand()` はGitコマンドの終了コードが0以外の場合に `NOT_GIT_REPOSITORY` を返すため、コミットのないGitリポジトリを `NO_COMMIT` として扱えるかは未解決である。
- 検索フォルダの登録、`.git`を持つclone候補の検索、remote URLによる一致判定は未実装である。
- CSV保存・復元は未実装である。
- `ActivityStreakService` の基本判定と `Main`・`Menu` への接続、3日「Git見習い」・7日「Gitマン」・14日「Gitスター」の表示、節目・記録切れメッセージは確認済みである。連続記録保護は今回の対象外である。
- 全角数字はメニュー番号・リポジトリ選択番号まで手動確認済みで、自動テストは未実装である。お世話の回復量は入力ではなく、種類ごとの固定値（ごはん20、なでる10）である。CSV保存と `Menu.start` 統合テストも未実装または未テストである。

2026-09-09時点で、上記のJava 21向けコンパイルと `MainTest` は成功している。CRUDで迷ったときは `AI展開資料/11-collections.md`、メニューの分岐で迷ったときは `05-control-flow.md`、メソッドの役割で迷ったときは `07-methods.md` を確認する。
